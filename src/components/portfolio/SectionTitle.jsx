const SectionTitle =({text})=>{
    return(
        <>
            <div className="border-b border-gray-200">
                <h2 className="text-3xl font-bold tracking-wider">{text}</h2>
            </div>
            SectionTitle
        </>
    )
}

export default SectionTitle